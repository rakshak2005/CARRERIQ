PORT=5000
DATABASE_URL=postgres://postgres:postgrespassword@localhost:5432/careeriq
NODE_ENV=development

# Optional: Firebase Admin configuration for real mode.
# If omitted or left as placeholder, the server runs in simulated mode.
FIREBASE_PROJECT_ID=placeholder_project_id
FIREBASE_CLIENT_EMAIL=placeholder_client_email
FIREBASE_PRIVATE_KEY=placeholder_private_key
